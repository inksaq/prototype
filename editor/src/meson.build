cpp_args = []
executable(
    'Editor',
    dependencies: [engine_dep],
    include_directories : '.',
    sources: files('./main.cpp'),
    cpp_args : cpp_args,
    install : true,
    install_dir: meson.source_root() / 'out'
)
